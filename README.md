# Sangam-Web
 Only for sangam

Commands to run:
#1 mvn clean install

for running on local:
mvn spring-boot:run

for packaging for deploy, run clean first:
mvn package
then upload the .jar file onto AWS, NOT the .jar.original file